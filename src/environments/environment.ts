// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

const firebaseConfig = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCbxeHAiDBl0oQ0X7sBFDI1rjsHqSrdkP4",
    authDomain: "sample-firebase-apps-87731.firebaseapp.com",
    projectId: "sample-firebase-apps-87731",
    databaseURL: "https://sample-api-2fee7-default-rtdb.firebaseio.com/",
    storageBucket: "sample-firebase-apps-87731.appspot.com",
    messagingSenderId: "895862750071",
    appId: "1:895862750071:web:cc90e7a732baf7b0e1bf72"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
